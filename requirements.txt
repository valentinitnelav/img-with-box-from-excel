# Usage: pip install -r requirements.txt
# But if you run the line below as suggested in the readme, then no need to care much about this file: 
# pip install "xlwings[all]"

pandas>=1.1.3
Pillow>=9.2.0
xlwings>=0.28.3

# Optional Dependencies for xlwings
# https://docs.xlwings.org/en/stable/installation.html#optional-dependencies
numpy>=1.19.2
matplotlib>=3.6.1
Jinja2>=2.11.1
requests>=2.23.0
